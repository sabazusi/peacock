# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'rabbithouse' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for rabbithouse
  pod 'SVProgressHUD'
  pod 'Fabric'
  pod 'TwitterKit'
  pod 'TwitterCore'
  pod 'SwiftyJSON'

  target 'rabbithouseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'rabbithouseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
